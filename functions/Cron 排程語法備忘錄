/*
 * =================================================================
 * Cron 排程語法備忘錄 (Cheatsheet)
 * =================================================================
 *
 * Cron 語法由五個欄位組成，用空格分隔，分別代表：
 *
 * ┌────────────── 分鐘 (0-59)
 * │ ┌──────────── 小時 (0-23)
 * │ │ ┌────────── 日期 (1-31)
 * │ │ │ ┌──────── 月份 (1-12)
 * │ │ │ │ ┌────── 星期 (0-7，0 和 7 都代表星期日)
 * │ │ │ │ │
 * * * * * *
 *
 * --- 特殊字元 ---
 * * : 代表「每一個單位」。
 * */n : 代表「每 n (單位)」。例如 */5 在分鐘欄位代表每 5 分鐘。
 * ,   : 用於列舉不連續的值。例如 1,3,5 在小時欄位代表 1 點、3 點和 5 點。
 * -   : 用於表示一個連續的範圍。例如 9-17 在小時欄位代表早上 9 點到下午 5 點。
 *
 * --- 常用範例 ---
 *
 * // --- 頻率 ---
 * '* * * * *'        // 每分鐘執行
 * '*/5 * * * *'      // 每 5 分鐘執行一次
 * '30 * * * *'       // 每小時的第 30 分鐘執行
 * '0 * * * *'        // 每個整點執行 (即每小時的第 0 分)
 *
 * // --- 每日 ---
 * '15 3 * * *'       // 每天凌晨 3:15 執行
 * '0 21 * * *'       // 每天晚上 9 點執行
 *
 * // --- 每週 ---
 * '0 4 * * 0'        // 每週日凌晨 4 點執行
 * '0 17 * * 1-5'     // 每個工作日 (週一至週五) 的下午 5 點執行
 * '0 12 * * 6,0'     // 每個週末 (週六、週日) 的中午 12 點執行
 *
 * // --- 每月 ---
 * '0 0 1 * *'        // 每月 1 號的凌晨 0 點執行
 *
 * // --- 複雜範例 ---
 * '0 8,18 * * *'     // 每天的早上 8 點和晚上 18 點各執行一次
 * '*/15 9-17 * * 1-5'// 工作日 (週一至週五) 的上午 9 點到下午 5 點之間，每 15 分鐘執行一次
 * '0 12 10 1 *'      // 每年 1 月 10 日的中午 12 點執行
 *
 */
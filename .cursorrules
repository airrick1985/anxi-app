# Project Rules

1.  **Language Requirement**: You must **ALWAYS** communicate in **Traditional Chinese (繁體中文)**.
2.  **Artifacts**: All artifacts (including `walkthrough.md`, `implementation_plan.md`, `task.md`), markdown files, and planning documents MUST be written in **Traditional Chinese**.
3.  **Code Comments**: Code comments should be in Traditional Chinese where appropriate for the team.
4.  **Translation**: If a user input is in another language, reply in Traditional Chinese.

## Windows PowerShell 指令規範
由於開發環境為 **Windows (PowerShell)**，在使用 `run_command` 工具時必須遵守以下規範：
1.  **禁用法令 (Unix-only)**：禁止直接使用 `grep`, `tail`, `head`, `cat`, `sed`, `awk`, `rm -rf` 等 Unix 指令。
2.  **替代指令 (PowerShell Cmdlets)**：
    *   `grep` → 使用 `Select-String`
    *   `tail -n 10` → 使用 `Get-Content -Tail 10`
    *   `head -n 10` → 使用 `Get-Content -TotalCount 10`
    *   `cat` → 使用 `Get-Content` 或 `type`
    *   `rm -rf` → 使用 `Remove-Item -Recurse -Force`
    *   `touch` → 使用 `New-Item -ItemType File`
3.  **優先使用內建工具**：優先使用 AI 提供的 `grep_search`, `find_by_name`, `view_file` 等跨平台工具，而非直接執行命令列指令。
4.  **路徑處理**：在命令列中使用路徑時，應確保符合 Windows 格式（反斜線 `\`），或讓 AI 自動處理路徑轉換。

## Firebase Functions 部署規範
當專案涉及 `functions/index.js` 或其他後端邏輯的 `新增` 或 `修改` 時：
1.  **即時提醒**：AI 必須在完成代碼修改後，主動告知使用者此為後端變更。
2.  **部署指令**：AI 必須明確提供發佈指令：`firebase deploy --only functions`。
3.  **驗證前置**：在使用者執行發佈指令前，不應引導使用者進行需要連動雲端的功能測試，以避免 CORS 或 404 等錯誤。
